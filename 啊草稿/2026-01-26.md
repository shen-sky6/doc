| **维度**        | **外网状态**                        | **内网状态**                        | **是否一致** |
| ------------- | ------------------------------- | ------------------------------- | -------- |
| **数据库字段**     | `file_storage_name` 存的是 UUID 路径 | `file_storage_name` 存的是 UUID 路径 | **一致**   |
| **MinIO 内部名** | 以 UUID 为文件名存储                   | 以 UUID 为文件名存储                   | **一致**   |
| **SFTP 物理名**  | UUID 名（用于摆渡）                    | UUID 名（待上传）                     | **一致**   |
| **用户感知名**     | 666.jpg（下载时动态指定）                | 666.jpg（下载时动态指定）                | **一致**   |


