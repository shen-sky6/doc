多线程实现方式在 Java 里非常丰富，不同方式适合不同场景。我帮你梳理一下主流的几种：

---

## 🔹 1. 继承 `Thread` 类

`class MyThread extends Thread {     @Override     public void run() {         System.out.println("线程运行：" + Thread.currentThread().getName());     } }  public class Main {     public static void main(String[] args) {         new MyThread().start();     } }`

- 适合快速写 demo。
    
- 不推荐大规模使用，因为 Java 不支持多继承。
    

---

## 🔹 2. 实现 `Runnable` 接口

`class MyRunnable implements Runnable {     @Override     public void run() {         System.out.println("线程运行：" + Thread.currentThread().getName());     } }  public class Main {     public static void main(String[] args) {         new Thread(new MyRunnable()).start();     } }`

- 更灵活，可以继承其他类。
    
- 但 `Runnable` 没有返回值。
    

---

## 🔹 3. 实现 `Callable` + `FutureTask`

`class MyCallable implements Callable<String> {     @Override     public String call() {         return "返回结果：" + Thread.currentThread().getName();     } }  public class Main {     public static void main(String[] args) throws Exception {         FutureTask<String> task = new FutureTask<>(new MyCallable());         new Thread(task).start();         System.out.println(task.get()); // 等待并获取结果     } }`

- 可以有返回值、可以抛出异常。
    
- 更适合需要计算结果的场景。
    

---

## 🔹 4. 线程池（推荐🔥）

`ExecutorService executor = Executors.newFixedThreadPool(4);  executor.submit(() -> {     System.out.println("任务执行：" + Thread.currentThread().getName()); });  executor.shutdown();`

- 线程池是**实际生产中最常用**的方式。
    
- 避免频繁创建销毁线程，节省资源。
    
- JDK 推荐使用 `ThreadPoolExecutor` 自定义线程池参数（核心数、队列、拒绝策略等）。
    

---

## 🔹 5. `CompletableFuture`（异步编排，推荐🔥）

·

- 可以链式编排任务，简洁强大。
    
- 适合异步调用、任务并行、结果合并。
    

---

## 🔹 6. 定时任务（ScheduledExecutorService）

`ScheduledExecutorService scheduler = Executors.newScheduledThreadPool(2); scheduler.scheduleAtFixedRate(() -> {     System.out.println("定时执行：" + Thread.currentThread().getName()); }, 1, 5, TimeUnit.SECONDS);`

- 可用于替代 `Timer`，支持多线程，精度更高。
    
- 适合定时任务、周期性执行。
    

---

## 🔹 7. 并行流（Stream API）

`List<Integer> list = Arrays.asList(1, 2, 3, 4, 5); list.parallelStream().forEach(i -> {     System.out.println(Thread.currentThread().getName() + " -> " + i); });`

- 简单并行计算场景用这个非常方便。
    
- 但不适合对线程数精细控制。
    

---

## 🔹 8. Fork/Join 框架

`ForkJoinPool pool = new ForkJoinPool(); pool.submit(() -> {     IntStream.range(1, 10).parallel().forEach(i ->         System.out.println(Thread.currentThread().getName() + " -> " + i)); }); pool.shutdown();`

- 适合递归、分治算法（比如大数组求和、并行计算）。
    

---

## 🔹 9. 协程（虚拟线程，Java 21 新特性）

`Thread.startVirtualThread(() -> {     System.out.println("虚拟线程：" + Thread.currentThread().getName()); });`

- JDK 21 开始支持虚拟线程（Project Loom）。
    
- 更轻量，适合 IO 密集型任务。
    

---

### ✅ 总结（生产推荐）

1. **线程池（ThreadPoolExecutor）** → 适合大部分场景
    
2. **CompletableFuture** → 适合异步调用、任务编排
    
3. **ScheduledExecutorService** → 定时任务
    
4. **虚拟线程（JDK21+）** → 高并发、IO 密集